_: {
  vim.spellcheck = {
    enable = true;
    ignoredFiletypes = ["toggleterm"];
    languages = ["en_au" "en_us"];
  };
  vim.options = {
    # Enable spell check highlighting by default
    spell = true;
  };
}
